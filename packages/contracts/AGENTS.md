# Shared contracts

Root orchestrator owns this boundary. A backend developer may receive an explicit, serial `contracts` task. Freeze and record the contract fingerprint before dispatching frontend/backend implementations. Contract changes invalidate dependent checks and reviews. Do not edit concurrently with either implementation stream.
