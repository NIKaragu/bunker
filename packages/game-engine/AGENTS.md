# Game engine

Backend scope owns implementation under the root workflow. Keep transitions independent of HTTP, Socket.IO, UI and wall-clock APIs. Acceptance criteria must cite verified rule IDs before gameplay implementation; the master prompt's research has not yet been performed.
